// Expresion ? si es true : si es false

let edad = 18;

let acceso = edad > 17 ? 'permitir ingreso' : 'no puede ingresasr';

console.log(acceso);

let peliculas = edad > 20 ? 'puede ver peliculas de miedo' : 'no puede ver';