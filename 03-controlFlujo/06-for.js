// Loop de for

let i = 0;


// Inicializamos un variable
// Comparamos
// Realizamos lo que queremos hacer con la misma

for (let a = i; a < 10; a++) {
  if (a % 2 === 0) {
    console.log(a + " el numero es par")
  }
}